<div class="row">
<div class="page-header">
  <h1><%= @location.name %> 
    <small><%= @location.city %>, 
    <%= @location.state %>
    </small>
  </h1>
</div>
  <%= render partial: 'locations/comments', locals: { comments: @comments } %>
</div>
